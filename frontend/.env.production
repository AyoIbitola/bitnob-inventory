# Production build talks directly to the backend. THIS REQUIRES the backend to
# send CORS headers for the deployed frontend origin (see DESIGN-NOTES). Point
# this at wherever the API is hosted at deploy time.
VITE_API_BASE_URL=https://bitnob-inventory.onrender.com
VITE_USE_MOCK_API=false
