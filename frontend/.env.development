# Dev: call the same-origin /api path so the Vite proxy handles it (no CORS).
VITE_API_BASE_URL=/api
# Backend the dev proxy forwards to (see vite.config.ts).
VITE_PROXY_TARGET=https://bitnob-inventory.onrender.com
